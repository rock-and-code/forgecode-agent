# ForgeCode Agent

ForgeCode Agent is a local-first, auditable open-source coding agent prototype.

This MVP slice includes a deterministic agent controller, fake model provider, approval policy, tool registry, run ledger, and a minimal `forgecode` CLI entry point for early development and testing.
